# Demo self-contained Python script to render a Quarto document using nbstata Jupyter kernel

To run this example you need

* uv installed
* Quarto installed
* Stata installed

Then to render the Quarto document run (from zsh/bash/Git Bash)

```sh
./render
```
